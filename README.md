# Petrov House — Toimisto
Live office dashboard for Petrov House.

## Pages
GitHub Pages from `/` (root): `index.html` + `status.json`

Poll `status.json` every few seconds. Characters stand still after 15 minutes without JSON write.
